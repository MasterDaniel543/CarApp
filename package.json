{
  "name": "car-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm ci --omit=dev"
  },
  "engines": {
    "node": "20.x"
  }
}